# File: .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches:
      - 17-08-2024changes  # Ensure this matches your default branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.6.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        run: |
          scp -o StrictHostKeyChecking=no deploy.sh user@13.233.152.98:/home/ubuntu/naavi_info13-08-24/deploy.sh
          ssh -o StrictHostKeyChecking=no user@13.233.152.98 'bash /home/ubuntu/naavi_info13-08-24/deploy.sh'
